# Nexora 极简使用指南

本指南介绍如何使用**桌面 GUI 客户端**连接组网，以及如何在 **Windows/macOS** 和 **Linux** 上部署**出口节点（全局代理网关）**。

---

## 一、 桌面客户端使用说明 (Windows / macOS)

### 1. 首次导入配置
1. 打开 `Nexora` 桌面客户端。
2. 点击 **“导入配置”**，选择管理员分发的 `config.json` 文件（或填入邀请码）。
3. 允许软件安装后台特权服务（仅首次需要授权）。

### 2. 一键连接与使用出口节点
* **开启连接**：点击主界面 **“连接”** 按钮，状态变为 **🟢 已连接** 并获取虚拟 IP（如 `100.64.0.5`）。
* **使用出口上网**：
  1. 在 **“出口节点选择”** 下拉列表中选择在线的出口设备。
  2. 勾选 **“开启全局路由”**，本机全部上网流量即加密通过该出口节点转发。

---

## 二、 如何部署出口节点 (Exit Node)

把家中、办公室或机房的设备部署为出口节点后，其他异地设备就能选择它代发上网流量。

### 1. Windows / macOS 桌面端开启
1. 打开客户端并导入配置。
2. 进入 **“设置”**，勾选 **“开启出口节点 (Exit Node)”** 并保存。

---

### 2. Linux 服务器 / 命令行开启

在 Linux 机器上，可通过以下两种最简单的方式加入并开启出口节点：

#### 方式 A：使用邀请码一键加入（最简单）
直接在终端运行以下命令：
```bash
sudo ./Nexora join-exit --server 你的服务器地址:443 --invite 你的邀请码
```

#### 方式 B：通过配置文件启动
在 Linux 的 `config.json` 配置文件中确保开启 `"exit_node": true`：
```json
{
  "role": "client",
  "addr": "你的服务器地址:443",
  "psk": "填入PSK",
  "uid": "填入本机UID",
  "client_private_key": "填入本机私钥",
  "exit_node": true,
  "accept_routes": true
}
```
运行启动命令：
```bash
sudo ./Nexora --config ./config.json
```

> **提示**：作为出口节点的 Linux 机器需保持开机联网，并确保防火墙未拦截 UDP 端口。
