# Nexora 极简使用指南（GUI 客户端与出口节点）

本指南介绍如何使用**桌面 GUI 客户端**连接组网，以及如何将一台电脑配置为**出口节点（网络代理网关）**。

---

## 一、 桌面客户端使用说明

### 1. 首次导入配置与授权
1. 打开 `Nexora` 桌面客户端。
2. 首次运行时，在弹出的界面中点击 **“导入配置”**，选择管理员提供给你的 `config.json` 配置文件（或填入邀请码）。
3. 按照系统提示，允许软件安装后台服务（仅首次需要管理员授权）。

### 2. 一键连接组网
* **开启连接**：点击主界面的 **“连接”** 大按钮（或在电脑右下角/顶部托盘图标上右键选择 **“连接”**）。
* **连接成功**：状态变为 **🟢 已连接**，界面将显示分配给你的虚拟 IP（例如 `100.64.0.5`）。此时你已与组网内的其他设备连通。

---

## 二、 如何使用出口节点（全局代理）

当你需要将所有上网流量通过某台指定的异地电脑代发（例如访问特定局域网或加密上网）时：

1. **连接组网**：确保客户端已处于已连接状态。
2. **选择出口节点**：在客户端界面的 **“出口节点选择”** 下拉菜单中，选择你想要使用的在线出口节点。
3. **开启全局网关**：勾选界面上的 **“开启全局路由”** 开关。
   * 开启后，你本机的公网上网流量将全部经过加密隧道，由所选的出口节点转发输出。
   * 随时取消勾选，即可切回本地正常上网。

---

## 三、 如何将电脑部署为“出口节点 (Exit Node)”

如果你想把家中或办公室的某台电脑部署为出口节点，供其他异地设备借道上网：

### 方法 1：通过桌面客户端直接开启（推荐）
1. 在作为出口节点的电脑上打开客户端并导入配置。
2. 点击右上角 **“设置”**，勾选 **“开启出口节点 (Exit Node)”** 选项。
3. 点击 **“保存并应用”**。此时该电脑即成功升级为出口节点，其他设备在列表里就能看到并使用它。

### 方法 2：通过配置文件开启
若该电脑使用配置文件启动，只需用记事本打开 `config.json`，确保包含以下字段：

```json
{
  "exit_node": true,
  "accept_routes": true
}
```

> **提示**：作为出口节点的电脑需要保持开机、联网且客户端处于连接状态。
